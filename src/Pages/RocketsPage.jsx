import React from 'react';
import RocketsList from '../Components/RocketsList';

const RocketsPage = () => {
    return (
        <div>
            <RocketsList />
        </div>
    );
}

export default RocketsPage;